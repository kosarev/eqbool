
def A
def B

assert_is (or) 0
assert_is (or 0) 0
assert_is (or 0 0 0) 0

assert_is (or A 1) 1

assert_is (or A ~A) 1

assert_is (or A A) A

assert_is (or A B) (or B A)

assert_is (or A (and A B)) A

assert_is (or A (or ~A B)) 1
